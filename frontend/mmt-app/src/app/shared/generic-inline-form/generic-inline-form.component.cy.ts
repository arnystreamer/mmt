import { GenericInlineFormComponent } from './generic-inline-form.component'

describe('GenericInlineFormComponent', () => {
  it('should mount', () => {
    cy.mount(GenericInlineFormComponent)
  })
})
