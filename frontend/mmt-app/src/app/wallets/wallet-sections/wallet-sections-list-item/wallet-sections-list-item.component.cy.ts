import { WalletSectionsListItemComponent } from './wallet-sections-list-item.component'

describe('WalletSectionsListItemComponent', () => {
  it('should mount', () => {
    cy.mount(WalletSectionsListItemComponent)
  })
})