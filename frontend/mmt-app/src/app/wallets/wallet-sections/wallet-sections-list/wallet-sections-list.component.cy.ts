import { WalletSectionsListComponent } from './wallet-sections-list.component'

describe('WalletSectionsListComponent', () => {
  it('should mount', () => {
    cy.mount(WalletSectionsListComponent)
  })
})