import { ReceiptEntryListItemComponent } from './receipt-entry-list-item.component'

describe('ReceiptEntryListItemComponent', () => {
  it('should mount', () => {
    cy.mount(ReceiptEntryListItemComponent)
  })
})